name: stickers_repository
description: Repository to manage your stickers.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  stickers_api:
    path:
      ../stickers_api
  uuid: ^4.4.0

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.19.2
  very_good_analysis: ^5.1.0
