name: in_memory_auth_api
description: Implementation of auth_repository_api.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  auth_api:
    path:
      ../auth_api
  crypto: ^3.0.3

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.19.2
  very_good_analysis: ^5.1.0
